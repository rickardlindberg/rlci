seq {
    stage(checkout)
    par {
        stage(build)
        stage(test)
        seq {
            stage(lint1)
            stage(lint2)
        }
    }
    stage(deploy)
}

seq {
    stage(one)
    stage(two)
    stage(three)
}
